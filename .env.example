# Copy to .env and fill in. Never commit .env.
PINECONE_API_KEY=

# Only needed for the real bulk-import path (migrate.py import --mode import).
# The integration ID comes from the Pinecone console:
#   https://app.pinecone.io/organizations/-/projects/-/storage
PINECONE_STORAGE_INTEGRATION_ID=
AWS_PROFILE=
